	===============VNAKL_
	
	NUM                 TNUM NOT NULL /* TNUM = INTEGER */, +
    PID                 TNUM NOT NULL /* TNUM = INTEGER */, +
    TOV_NAME            TTOVAR_NAME COLLATE WIN1251_UA /* TTOVAR_NAME = VARCHAR(300) */, +
    TOV_KOLVO           TKOLVO /* TKOLVO = DOUBLE PRECISION */, +
    TOV_CENA            TMONEY /* TMONEY = DOUBLE PRECISION */, +
    TOV_ED              TED_IZM COLLATE WIN1251_UA /* TED_IZM = VARCHAR(10) */, +
    TOV_CENA_PDV        TMONEY /* TMONEY = DOUBLE PRECISION */, +
    TOV_KOLVO_PART      TNUM /* TNUM = INTEGER */, +
    TOVAR_ID            TNUM /* TNUM = INTEGER */, +
    CURR_TOV_CENA       TMONEY /* TMONEY = DOUBLE PRECISION */, +
    CURR_TOV_CENA_PDV   TMONEY /* TMONEY = DOUBLE PRECISION */, +
    SKLAD_ID            TNUM /* TNUM = INTEGER */, +
    TOV_SUMA            TMONEY /* TMONEY = DOUBLE PRECISION */, +
    CURR_TOV_SUMA       TMONEY /* TMONEY = DOUBLE PRECISION */, +
    DOC_DOPOLN          TMEMO /* TMEMO = VARCHAR(300) */,
    IS_PDV              TBOOL DEFAULT -1 /* TBOOL = SMALLINT */,
    COMPL_ID            INTEGER DEFAULT 0,
    IS_COMPL            INTEGER DEFAULT 0,
    COMPL_KOLVO_DEF     DOUBLE PRECISION,
    COMPL_IS_CONST      INTEGER DEFAULT 0,
    TOV_SUMA_ZNIG       TMONEY DEFAULT 0 /* TMONEY = DOUBLE PRECISION */,
    CURR_TOV_SUMA_ZNIG  TMONEY DEFAULT 0 /* TMONEY = DOUBLE PRECISION */
	
	
	
	
	
	
	VNAKL (
    NUM                  TNUM NOT NULL /* TNUM = INTEGER */, +
    FIRMA_ID             TNUM /* TNUM = INTEGER */, +
    NU                   TSM_KOD COLLATE WIN1251_UA /* TSM_KOD = VARCHAR(30) */, +
    DATE_DOK             TDATE /* TDATE = TIMESTAMP */, +
    CLIENT               TFIO COLLATE WIN1251_UA /* TFIO = VARCHAR(300) */, +
    CENA                 TMONEY /* TMONEY = DOUBLE PRECISION */, +
    CENA_PDV             TMONEY /* TMONEY = DOUBLE PRECISION */, +
    CENA_ZNIG            TMONEY /* TMONEY = DOUBLE PRECISION */, +
    DOV_SER              TDOV_KOD /* TDOV_KOD = VARCHAR(15) */, 
    DOV_NUM              TDOV_KOD /* TDOV_KOD = VARCHAR(15) */,
    DOV_DATA             TDATE /* TDATE = TIMESTAMP */,
    DOV_FIO              TMEDFIO /* TMEDFIO = VARCHAR(100) */,
    CLIENT_ID            TNUM /* TNUM = INTEGER */, +
    PDV                  TMONEY /* TMONEY = DOUBLE PRECISION */, +
    PROD_UMOV            TTEXT /* TTEXT = VARCHAR(150) */,
    ZNIG_TYPE            TBOOL /* TBOOL = SMALLINT */, +
    POD_REKL_PER         TMONEY /* TMONEY = DOUBLE PRECISION */,
    POD_REKL_CH          TMONEY /* TMONEY = DOUBLE PRECISION */, +
    FIO_BUH              TKOD /* TKOD = VARCHAR(50) */,
    NU_ID                TNUM /* TNUM = INTEGER */, +
    SKLAD_ID             TNUM /* TNUM = INTEGER */, +
    CURR_TYPE            TNUM /* TNUM = INTEGER */, +
    CURR_CENA            TMONEY /* TMONEY = DOUBLE PRECISION */, +
    CURR_CENA_PDV        TMONEY /* TMONEY = DOUBLE PRECISION */, +
    CURR_CENA_ZNIG       TMONEY /* TMONEY = DOUBLE PRECISION */, +
    CURR_PDV             TMONEY /* TMONEY = DOUBLE PRECISION */, +
    CURR_POD_REKL_CH     TMONEY /* TMONEY = DOUBLE PRECISION */, 
    CLIENT_RAH_ID        TNUM /* TNUM = INTEGER */,
    AFIRM_RAH_ID         TNUM /* TNUM = INTEGER */,
    DOPOLN               TMEMO /* TMEMO = VARCHAR(300) */,
    CENA_TOV_TRANS       TMONEY default 0 /* TMONEY = DOUBLE PRECISION */,
    CURR_CENA_TOV_TRANS  TMONEY default 0 /* TMONEY = DOUBLE PRECISION */,
    IS_MOVE              TBOOL default 1 /* TBOOL = SMALLINT */, +
    DOC_MARK_TYPE        TNUM DEFAULT 0 /* TNUM = INTEGER */,
    DOC_USER_ID          TNUM NOT NULL /* TNUM = INTEGER */, +
    DOC_CREATE_TIME      TDATE DEFAULT CURRENT_TIMESTAMP /* TDATE = TIMESTAMP */, +
    DOC_MODIFY_TIME      TDATE DEFAULT CURRENT_TIMESTAMP /* TDATE = TIMESTAMP */, +
    DOC_DESCR            TTEXT /* TTEXT = VARCHAR(150) */,
    ARTICLE_ID           TNUM DEFAULT 0 /* TNUM = INTEGER */,
    TTN_NU               TSM_KOD /* TSM_KOD = VARCHAR(30) */,
    TTN_DATE_DOK         TDATE /* TDATE = TIMESTAMP */,
    TTN_DOR_LIST         TSM_KOD /* TSM_KOD = VARCHAR(30) */,
    TTN_AVTO             TKOD /* TKOD = VARCHAR(50) */,
    TTN_AVTO_PIDPR       TFIO /* TFIO = VARCHAR(300) */,
    TTN_VODITEL          TFIO /* TFIO = VARCHAR(300) */,
    TTN_ADR_FROM         TADR /* TADR = VARCHAR(200) */,
    TTN_ADR_TO           TADR /* TADR = VARCHAR(200) */,
    IM_NUM               TNUM DEFAULT -1 /* TNUM = INTEGER */, -1
    ZNIG_PROC            TMONEY DEFAULT 0 /* TMONEY = DOUBLE PRECISION */,
    TTN_AVTO_PRIC        TKOD /* TKOD = VARCHAR(50) */,
    TTN_VID_PEREV        TKOD /* TKOD = VARCHAR(50) */,
    TTN_KOLVO_MIS        TKOD /* TKOD = VARCHAR(50) */,
    TTN_MAS_BRUT         TKOD /* TKOD = VARCHAR(50) */,
    PDV_TYPE             TNUM /* TNUM = INTEGER */
);